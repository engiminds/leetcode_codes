            // Check overflow before multiplying by 10

            x /= 10;
        while (x != 0) {
            int digit = x % 10;

    int reverse(int x) {
        int rev = 0;
public:
class Solution {
